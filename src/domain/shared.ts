export type IsoDate = `${number}-${number}-${number}`;

export type Range = {
  min: number;
  max: number;
  unit: string;
};

export type DisplayTone = "green" | "blue" | "amber" | "red" | "neutral";
